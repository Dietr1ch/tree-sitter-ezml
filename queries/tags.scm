
(element
  (element_name) @definition.class)

(element
  (element_name)
  (element_id) @name) @definition.id

(element
  (element_name)
  (tag
   (tag_name) @name)) @definition.tag

(element
  (element_name)
  (attribute
   (attribute_name) @name
   (attribute_value) @definition.value)) @definition.attribute
